---
// A small, escaped-text formatter for editorial emphasis; never inject HTML.
interface Props { text: string; }
const { text } = Astro.props;
const parts = text.split(/\*\*(.+?)\*\*/g);
---
<p>{parts.map((part, index) => index % 2 ? <strong>{part}</strong> : part)}</p>
